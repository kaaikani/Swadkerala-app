name: fluttertoast
description: Minimal stub to satisfy razorpay_flutter dependency
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=2.17.6 <4.0.0"
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  # No plugin section - pure Dart package to avoid native build errors

