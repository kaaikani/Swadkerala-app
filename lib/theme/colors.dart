import 'package:flutter/material.dart';
import 'dart:ui';


class AppColors {
  static const Color icon= Colors.black;
  static const Color primary = Colors.red;

  static const Color primaryLight = Colors.redAccent; // lighter variant
  static const Color accent = Color(0xFFFFA000);
  static const Color background = Color(0xFFF5F5F5);
  static const Color card = Colors.white;
  static const Color heartActive = Colors.redAccent;
  static const Color heartInactive = Colors.black;
  static const Color buttonText = Colors.white;
  static const Color error = Color(0xFFE53935); // 🔴 Add this line
}
