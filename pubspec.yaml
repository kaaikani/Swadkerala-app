name: unified_ecomapp
description: "Unified E-commerce Application"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  # Core dependencies only
  cupertino_icons: ^1.0.8
  graphql_flutter: ^5.1.2
  gql: ^1.0.1
  flutter_dotenv: ^6.0.0
  get: ^4.7.2
  get_storage: ^2.1.1
  connectivity_plus: ^5.0.2
  razorpay_flutter: ^1.3.7
  mobile_number: ^3.0.0
  permission_handler: ^11.3.1




dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.6
  graphql_codegen: ^2.0.0


flutter:
  uses-material-design: true



  assets:
    - .env
